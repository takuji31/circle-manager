mutation UpdateMemberMonthCircle(
  $memberId: String!
  $year: String!
  $month: String!
  $circleId: String!
) {
  updateMemberMonthCircle(
    memberId: $memberId
    year: $year
    month: $month
    circleId: $circleId
  ) {
    monthCircle {
      ...MemberMonthCircle
    }
  }
}
