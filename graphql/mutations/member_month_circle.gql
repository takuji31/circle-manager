mutation UpdateMemberMonthCircle(
  $memberId: String!
  $year: Int!
  $month: Int!
  $circleId: String!
) {
  updateMemberMonthCircle(
    memberId: $memberId
    year: $year
    month: $month
    circleId: $circleId
  ) {
    monthCircle {
      ...MemberMonthCircle
    }
  }
}
