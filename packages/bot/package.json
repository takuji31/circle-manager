{
  "name": "bot",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "database": "1.0.0",
    "discord.js": "^13.6.0",
    "dotenv": "^16.0.0",
    "model": "1.0.0"
  },
  "scripts": {
    "build": "tsc --build",
    "build:clean": "tsc --build --clean && tsc --build"
  }
}
