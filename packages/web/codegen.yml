overwrite: true
schema: 'graphql/schema.graphqls'
documents: 'graphql/**/*.gql'
generates:
  src/graphql/generated/type.ts:
    plugins:
      - add:
          content: "import { LocalDate } from '../../model/date';"
      - 'typescript'
      - 'typescript-operations'
      - typed-document-node
    config:
      dedupeFragments: true
      scalars:
        Date: LocalDate
